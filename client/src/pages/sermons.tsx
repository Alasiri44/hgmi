import SermonsList from "../features/sermonsList";

export default function Sermons(){
    return (
        <div>
            <h1>Sermons of Higher Grounds Ministries International</h1>
            <p>This is the sermons page.</p>
            < SermonsList/>
        </div>
    )
}